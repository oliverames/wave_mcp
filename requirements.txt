# Wave Accounting MCP Server dependencies

# MCP framework. 1.2+ provides FastMCP, which this server is built on.
mcp>=1.2.0

# Async HTTP client for the GraphQL transport
httpx>=0.24.0

# Loads WAVE_ACCESS_TOKEN from a .env file
python-dotenv>=1.0.0

# Development only:
#   pytest>=7.0      test suite
#   pyflakes>=3.0    lint
